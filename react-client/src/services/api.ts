// API wrapper with axios or fetch
export const api = {
  get: async (url: string) => {},
  post: async (url: string, data: any) => {},
  put: async (url: string, data: any) => {},
  delete: async (url: string) => {},
};
